{
  "name": "reason-node-b",
  "scripts": {
    "build": "bsb -backend bytecode",
    "examples": "./lib/bs/bytecode/example.byte",
    "tests": "./lib/bs/bytecode/test.byte",
    "cleanB": "bsb -backend bytecode -clean-world",
    "cleanN": "bsb -backend native -clean-world",
    "devTest": "npm run cleanB && npm run build && npm run testsB",
    "devExample": "npm run cleanB && npm run build && npm run examples",
    "watch": "bsb -backend bytecode -w"
  },
  "devDependencies": {
    "bs-platform": "github:bsansouci/bsb-native"
  }
}
