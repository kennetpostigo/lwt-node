(jbuild_version 1)

(executable
 ((name test)
  (libraries (ReasonNode alcotest))
    (preprocess (pps (bisect_ppx -conditional)))))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (run ${<}))))
